# NOTE: Derived from blib/lib/PDF/Reuse.pm.
# Changes made here will be lost when autosplit is run again.
# See AutoSplit.pm.
package PDF::Reuse;

#line 3626 "blib/lib/PDF/Reuse.pm (autosplit into blib/lib/auto/PDF/Reuse/prLogDir.al)"
sub prLogDir
{  $ldir = findDir(shift);
   1;
}

# end of PDF::Reuse::prLogDir
1;
